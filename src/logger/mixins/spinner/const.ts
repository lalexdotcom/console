/** Randomisation range applied to the spinner interval (±ms). */
export const SPINNER_INTERVAL_JITTER = 150; //ms.

/** A single-frame icon with an optional associated color. */
export type SpinnerIcon = { icon: string; color?: string };
