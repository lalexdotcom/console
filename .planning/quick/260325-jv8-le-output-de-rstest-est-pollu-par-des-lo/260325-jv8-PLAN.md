---
phase: quick-260325-jv8
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/node/main/spinner-node.test.ts
  - src/worker/index.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "pnpm test:node affiche uniquement les résultats rstest, sans logs parasites"
    - "Les 4 tests spinner concernés continuent de passer"
    - "Le fork du worker n'écrit plus sur le stderr hérité du processus parent"
  artifacts:
    - path: tests/node/main/spinner-node.test.ts
      provides: "Tests spinner sans fuite vers process.stdout"
    - path: src/worker/index.ts
      provides: "Fork avec stderr pipé (non hérité)"
  key_links:
    - from: tests/node/main/spinner-node.test.ts
      to: process.stdout
      via: "captureAll() wrapping spinner creation"
      pattern: "captureAll.*spin"
    - from: src/worker/index.ts
      to: child process stderr
      via: "stdio fork option fd[2]"
      pattern: "stdio.*pipe"
---

<objective>
Supprimer la pollution de l'output rstest causée par deux sources indépendantes : des spinners créés hors de `captureAll()` dans les tests, et le stderr hérité du processus worker forké.

Purpose: Rendre l'output `pnpm test:node` lisible — uniquement les résultats rstest, sans frames spinner parasites ni erreurs `ERR_UNKNOWN_FILE_EXTENSION`.
Output: Tests propres, fork silencieux.
</objective>

<execution_context>
@.github/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@tests/node/main/spinner-node.test.ts
@src/worker/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1 — Wrapper les 4 créations de spinner dans captureAll()</name>
  <files>tests/node/main/spinner-node.test.ts</files>
  <action>
    Les 4 tests suivants instancient un spinner HORS de `captureAll()`, ce qui écrit le frame initial `[ ⋯ ]` directement sur `process.stdout` lors des runs rstest.

    Appliquer le pattern suivant pour chacun :

    ```ts
    // ❌ Avant (fuite sur stdout)
    const sp = L.scope('spin-01-stop').info.spin('task');
    const { stdout } = captureAll(() => sp.stop());

    // ✅ Après (sans fuite)
    let sp!: LoggerSpinner;
    captureAll(() => { sp = L.scope('spin-01-stop').info.spin('task'); });
    const { stdout } = captureAll(() => sp.stop());
    ```

    **Test spin-01-stop** (~ligne 83) :
    - Déclarer `let sp!: LoggerSpinner;`
    - Wrapper `sp = L.scope('spin-01-stop').info.spin('task');` dans `captureAll(() => { ... });`
    - Garder `const { stdout } = captureAll(() => sp.stop());` inchangé

    **Test spin-02-after-stop** (~ligne 93) :
    - Déclarer `let sp!: LoggerSpinner;`
    - Wrapper la création ET `sp.stop()` dans un premier `captureAll`
    - Garder les deux `captureAll` d'assertion (`sp.success()`, `sp.fail()`) inchangés

    **Test spin-02-double-success** (~ligne 101) :
    - Déclarer `let sp!: LoggerSpinner;`
    - Wrapper `sp = L.scope('spin-02-double-success').info.spin('task');` dans `captureAll(() => { ... });`
    - Garder `captureAll(() => sp.success('first'));` et le `captureAll` d'assertion inchangés

    **Test spin-08-stderr** (~ligne 249) :
    - Déclarer `let sp!: LoggerSpinner;`
    - Wrapper `sp = L.scope('spin-08-stderr').error.spin('loading');` dans `captureAll(() => { ... });`
    - Garder `const { stderr } = captureAll(() => sp.success('done'));` inchangé

    Le type `LoggerSpinner` est déjà importé dans le fichier — ne pas ajouter d'import.
  </action>
  <verify>
    <automated>pnpm test:node --reporter=verbose 2>&1 | grep -E "spin-01-stop|spin-02-after-stop|spin-02-double-success|spin-08-stderr"</automated>
  </verify>
  <done>Les 4 tests passent. Aucun frame `⋯` ne s'affiche hors des blocs captureAll dans l'output.</done>
</task>

<task type="auto">
  <name>Task 2 — Piper le stderr du fork worker (ne plus hériter fd 2)</name>
  <files>src/worker/index.ts</files>
  <action>
    Dans `createNodeTransport()` (~ligne 237), changer uniquement fd 2 dans l'option `stdio` du fork :

    ```ts
    // ❌ Avant
    stdio: ['inherit', 'inherit', 'inherit', 'ipc'],

    // ✅ Après
    stdio: ['inherit', 'inherit', 'pipe', 'ipc'],
    ```

    Mettre à jour le commentaire au-dessus du `fork()` pour refléter ce changement :

    ```ts
    // Pass the same stdin/stdout file descriptors to the child so it becomes
    // the sole owner of the TTY. Stderr is piped (not inherited) so any
    // accidental worker stderr does not bleed into the parent terminal or test
    // output. IPC is added as fd[3].
    ```

    Ne pas lire `child.stderr` — le laisser silencieux est le comportement voulu (les workers ne doivent pas écrire sur stderr en production ; en tests, les erreurs comme ERR_UNKNOWN_FILE_EXTENSION sont déjà couvertes par `child.on('error', ...)`).

    Ne modifier rien d'autre dans ce fichier.
  </action>
  <verify>
    <automated>pnpm test:node 2>&1 | grep -c "ERR_UNKNOWN_FILE_EXTENSION" || true</automated>
  </verify>
  <done>La commande ci-dessus retourne 0 (aucune occurrence). La suite complète passe : `pnpm test:node` sans erreurs de type.</done>
</task>

</tasks>

<verification>
Run complet après les deux tâches :

```bash
pnpm test:node 2>&1 | tail -20
```

Attendu : uniquement le résumé rstest (ex. `171 passed`), sans lignes `[ ⋯ ]` parasites ni `ERR_UNKNOWN_FILE_EXTENSION`.
</verification>

<success_criteria>
- `pnpm test:node` affiche un output propre : résultats rstest uniquement
- Tous les tests spinner (spin-01 à spin-08) passent
- Aucune régression sur les 171 tests existants
</success_criteria>

<output>
Après complétion, créer `.planning/quick/260325-jv8-le-output-de-rstest-est-pollu-par-des-lo/260325-jv8-SUMMARY.md`
</output>
